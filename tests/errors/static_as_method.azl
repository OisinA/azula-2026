// error: is a static function: call it as Point::origin()
struct Point {
    x: int;

    static func origin(): Point {
        return Point { x: 0 };
    }
}
func main {
    var p = Point::origin();
    var q = p.origin();
}
