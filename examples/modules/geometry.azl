struct Rect {
    width: int;
    height: int;

    func area(): int {
        return self.width * self.height;
    }

    func perimeter(): int {
        return 2 * (self.width + self.height);
    }
}

func square(side: int): Rect {
    return Rect { width: side, height: side };
}
